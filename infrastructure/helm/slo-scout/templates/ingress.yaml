{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "slo-scout.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "slo-scout.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      # Backend API paths (must be before frontend)
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: {{ include "slo-scout.fullname" . }}-backend
            port:
              number: 8000
      - path: /docs
        pathType: Prefix
        backend:
          service:
            name: {{ include "slo-scout.fullname" . }}-backend
            port:
              number: 8000
      - path: /openapi.json
        pathType: Prefix
        backend:
          service:
            name: {{ include "slo-scout.fullname" . }}-backend
            port:
              number: 8000
      # Frontend - root path (last to not override backend paths)
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "slo-scout.fullname" . }}-frontend
            port:
              number: 80
{{- end }}
