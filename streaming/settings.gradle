pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'slo-scout-streaming'

include 'fingerprinting-job'
include 'embedding-pipeline-job'
